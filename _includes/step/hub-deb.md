#### Installing EmonHub with Debian package installer

    sudo apt-get update
    sudo apt-get --force-yes -y install emonhub
    
*note - add any {% include link/int/pack-list %} to be installed at the same time to the command above*
    
